import { setRequestLocale, getTranslations } from 'next-intl/server';
import { StaticPage } from '@/components/layout/static-page';

export default async function SupportPage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  setRequestLocale(locale);
  const t = await getTranslations('nav');

  return (
    <StaticPage title={t('support')}>
      <h2>Обращение в поддержку</h2>
      <p>
        При возникновении технических затруднений обратитесь по адресу{' '}
        <a href="mailto:support@example.kz">support@example.kz</a>. В обращении укажите
        свой e-mail, наименование дисциплины и подробное описание проблемы.
      </p>

      <h2>Частые вопросы</h2>

      <h3>Не удаётся войти</h3>
      <p>
        Учётные записи создаёт администратор — открытая саморегистрация не предусмотрена.
        Если пароль забыт, обратитесь в учебный отдел для его сброса. После пяти неудачных
        попыток вход блокируется на 15 минут.
      </p>

      <h3>Не засчитывается время работы с материалом</h3>
      <p>
        Время засчитывается только при активном взаимодействии со страницей. При отсутствии
        активности более 5 минут сессия закрывается, и неактивное время не учитывается.
        На один элемент засчитывается не более 150 % его плановой трудоёмкости.
      </p>

      <h3>Прервалась связь во время теста</h3>
      <p>
        Промежуточное состояние попытки сохраняется. Вернитесь на страницу теста — порядок
        вопросов и уже данные ответы сохранятся. Обратите внимание, что отсчёт времени
        на выполнение теста при этом не останавливается.
      </p>

      <h3>Видео не воспроизводится</h3>
      <p>
        Проверьте, не блокирует ли расширение браузера встраиваемое видео. Учёт просмотра
        ведётся по фактически просмотренной доле ролика, а не по времени нахождения на странице.
      </p>

      <h2>Поддерживаемые браузеры</h2>
      <p>
        Последние две версии Chrome, Firefox, Safari и Edge. Все сценарии обучения работоспособны
        на экране шириной от 360 px — курс можно проходить со смартфона.
      </p>
    </StaticPage>
  );
}
